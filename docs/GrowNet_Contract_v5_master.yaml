# DEPRECATED (legacy format)
# This file has been archived to: docs/archive/contracts/GrowNet_Contract_v5_master.yaml
# The authoritative GrowNet contract is now machine-first JSON:
#   docs/contracts/grownet.contract.v5.json
#
# Rationale: the contract is primarily consumed by automation/AI and must be unambiguous
# and schema-validated to prevent cross-language divergence.
